import { DiplayProductDescriptionPipe } from './diplay-product-description.pipe';

describe('DiplayProductDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DiplayProductDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
