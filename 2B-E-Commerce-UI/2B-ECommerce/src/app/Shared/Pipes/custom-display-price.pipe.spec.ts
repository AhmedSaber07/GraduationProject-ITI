import { CustomDisplayPricePipe } from './custom-display-price.pipe';

describe('CustomDisplayPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDisplayPricePipe();
    expect(pipe).toBeTruthy();
  });
});
